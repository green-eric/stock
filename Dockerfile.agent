FROM python:3.9-alpine

WORKDIR /app

# 复制依赖文件
COPY requirements.txt .

# 安装依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . .

# 启动Agent服务（具体命令由docker-compose.yml指定）
CMD ["python", "-m", "agents.data_agent"]